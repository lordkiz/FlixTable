import {SafeAreaView} from 'react-native';
import {Home} from './src/screens/Home';

const App = (): JSX.Element => {
  return (
    <SafeAreaView>
      <Home />
    </SafeAreaView>
  );
};

export default App;
